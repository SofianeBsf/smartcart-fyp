# Email links (verification, password reset, order details)
BASE_URL=http://localhost:3000

# Database - local PostgreSQL
DATABASE_URL=postgresql://smartcart:smartcart_password@localhost:5432/smartcart

# Auth
JWT_SECRET=to-change-to-a-secure-random-string
OWNER_OPEN_ID=dev-admin

# Chatbot LLM (any OpenAI-compatible endpoint — OpenAI, Google Gemini, etc.)
BUILT_IN_FORGE_API_URL=https://generativelanguage.googleapis.com/v1beta/openai/chat/completions
BUILT_IN_FORGE_API_KEY=my-api-key-here
BUILT_IN_FORGE_MODEL=gemini-2.5-flash

# Email (Resend) - preferred, works from cloud hosts that block SMTP (e.g. Render)
RESEND_API_KEY=re_my_api_key_here
RESEND_FROM_EMAIL=my_from_email_address

# Gmail SMTP via Nodemailer
SMTP_USER=my-gmail@gmail.com
SMTP_PASS=my-16-char-app-password